<UserControl x:Class="View.Controls.ContactControl"
			xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			mc:Ignorable="d"
			d:DesignHeight="450" d:DesignWidth="800">
	<UserControl.Resources>
		<Style TargetType="TextBlock">
			<Setter Property="Margin" Value="3"/>
		</Style>
		<Style TargetType="TextBox">
			<Setter Property="Margin" Value="3"/>
			<Setter Property="Background" Value="White"/>
			<Setter Property="ToolTip" Value="{x:Null}"/>
			<Style.Triggers>
				<Trigger Property="Validation.HasError" Value="true">
					<Setter Property="ToolTip"
                            Value="{Binding RelativeSource={x:Static RelativeSource.Self},
                            Path=(Validation.Errors)[0].ErrorContent}"/>
				</Trigger>
				<Trigger Property="Validation.HasError" Value="true">
					<Setter Property="Background" Value="LightPink"/>
				</Trigger>
			</Style.Triggers>
		</Style>
		<ControlTemplate x:Key="ValidationErrorTemplate">
			<Border>
				<AdornedElementPlaceholder/>
			</Border>
		</ControlTemplate>
	</UserControl.Resources>
	<DockPanel Grid.Column="2" Margin="8">
        <Image Source="\Resources\photo_placeholder_100x100.png" DockPanel.Dock="Left" VerticalAlignment="Top"
               Width="100" Height="100" Margin="0 25 0 0"/>
		<StackPanel>
			<TextBlock Text="Name:"/>
			<TextBox Text="{Binding SelectedContact.Name, UpdateSourceTrigger=PropertyChanged}"
                     IsReadOnly="{Binding ReadMode}"
                     Validation.ErrorTemplate="{StaticResource ValidationErrorTemplate}"/>
			<TextBlock Text="Phone Number:"/>
			<TextBox Text="{Binding SelectedContact.Phone, UpdateSourceTrigger=PropertyChanged}"
                     PreviewTextInput="TextBox_PreviewTextInput"
                     DataObject.Pasting="TextBox_Pasting"
                     IsReadOnly="{Binding ReadMode}"
                     Validation.ErrorTemplate="{StaticResource ValidationErrorTemplate}"/>
			<TextBlock Text="Email:"/>
			<TextBox Text="{Binding SelectedContact.Email, UpdateSourceTrigger=PropertyChanged}"
                     IsReadOnly="{Binding ReadMode}"
                     Validation.ErrorTemplate="{StaticResource ValidationErrorTemplate}"/>
		</StackPanel>
	</DockPanel>
</UserControl>