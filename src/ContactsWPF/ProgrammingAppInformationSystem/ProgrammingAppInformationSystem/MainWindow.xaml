<Window x:Class="ProgrammingAppInformationSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProgrammingAppInformationSystem"
        xmlns:model="clr-namespace:ProgrammingAppInformationSystem.Model"
        mc:Ignorable="d"
        Title="Contact Manager" Height="450" Width="800" MinHeight="450" MinWidth="800"
        Icon="\ViewModel\contacts_96x96.ico">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel -->
        <Grid Grid.Column="0" Background="#f8f8f8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Search section - now in one line -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="8" VerticalAlignment="Center">
                <Label Content="Find:" Margin="3,0,3,0" VerticalAlignment="Center"/>
                <TextBox Width="150" Height="25" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Contacts list -->
            <ListBox Grid.Row="1" Margin="8" ItemsSource="{Binding Contacts}" 
                    SelectedItem="{Binding SelectedContact}" 
                    DisplayMemberPath="Name"/>

            <!-- Buttons at the bottom -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="8" HorizontalAlignment="Center">
                <Button Content="Add" Command="{Binding AddCommand}" Margin="3" Height="25" Width="75"/>
                <Button Content="Edit" Command="{Binding EditCommand}" Margin="3" Height="25" Width="75"/>
                <Button Content="Remove" Command="{Binding RemoveCommand}" Margin="3" Height="25" Width="75"/>
            </StackPanel>
        </Grid>

        <!-- Grid Splitter -->
        <GridSplitter Grid.Column="1" Width="3" Background="White" HorizontalAlignment="Stretch"/>

        <!-- Right Panel -->
        <Grid Grid.Column="2" Background="White">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Photo placeholder -->
                <Image Grid.Row="0" Grid.Column="0" Width="100" Height="100" Margin="3" 
                            Source="\ViewModel\photo_placeholder_100x100.png"
                            Grid.RowSpan="2"/>

                <!-- Name and Email closer together -->
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="3">
                    <TextBlock Text="Name:"/>
                    <TextBox Text="{Binding EditingContact.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                             IsReadOnly="{Binding IsNotEditing}" Margin="0,3,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Margin="3">
                    <TextBlock Text="Email:"/>
                    <TextBox Text="{Binding EditingContact.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                             IsReadOnly="{Binding IsNotEditing}" Margin="0,3,0,0"/>
                </StackPanel>

                <!-- Phone with more space below -->
                <StackPanel Grid.Row="2" Grid.Column="1" Margin="3">
                    <TextBlock Text="Phone:"/>
                    <TextBox Text="{Binding EditingContact.Phone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                             IsReadOnly="{Binding IsNotEditing}" Margin="0,3,0,0"/>
                </StackPanel>

                <!-- Apply button -->
                <Button Grid.Row="4" Grid.Column="1" Content="Apply" Command="{Binding ApplyCommand}" 
                        HorizontalAlignment="Right" Margin="0,50,0,0" Width="75" Height="25"
                        Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibility}}"/>
            </Grid>
        </Grid>
    </Grid>
</Window>